{
  "name": "thoughtbox",
  "version": "1.0.0",
  "description": "AI-powered thought capture and organisation app",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "cd frontend && npm run dev && cd .. && cd backend && npm run dev"
  }
}
